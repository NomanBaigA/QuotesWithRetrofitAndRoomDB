package com.example.quoteswithretrofitandroomdb.models

import androidx.room.Entity
import androidx.room.PrimaryKey

// STEP-1 this class is generated by kotlin from json converter tool

// STEP-7 (Step 1 fro room implementation) For room related work we are using it but we can create separate one too
@Entity(tableName = "quote")
data class Result(
    @PrimaryKey(autoGenerate = true)
    val quoteId: Int,
    val _id: String,
    val author: String,
    val authorSlug: String,
    val content: String,
    val dateAdded: String,
    val dateModified: String,
    val length: Int
)